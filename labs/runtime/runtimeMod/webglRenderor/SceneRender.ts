
import scene = require('../scene/Scene');
import cam = require('../webglRenderor/Camera')

export class SceneRender{
    constructor(){
        
    }
    
    /**
     * cam没有的话，则用scene指定的
     */
    static render(sce:scene.Scene,cam?:cam.Camera){
        
    }
}