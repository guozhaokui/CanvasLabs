
import model = require('./Model')
import cam = require('../webglRenderor/Camera')

export class Scene{
    constructor(){
    }
    
    addMod(mod:model.Model,pos?:Float32Array, rmat?:Float32Array){
        
    }
    addCam(cam:cam.Camera){
        
    }
}

